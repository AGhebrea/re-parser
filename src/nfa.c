#include <nfa.h>

/* The whole purpose of writing this parser was to build these NFAs
 * It should be template driven and we can just do it by hand. 
 * We just need to do the examples by hand for each operation 
 * type that our grammar supports */
void buildNFA(expressionRegularExpression_t* expression)
{

}